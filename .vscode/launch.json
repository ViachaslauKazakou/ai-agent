{
    // Конфигурация предназначена для расширения CodeLLDB.
    // Cargo-проект находится в корне workspace.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ai-agent",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/ai-agent",
            "args": [
                "${input:agentArgs}"
            ],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": [
                "rust"
            ],
            "env": {
                "RUST_BACKTRACE": "1"
            },
            "cargo": {
                "args": [
                    "build",
                    "--manifest-path",
                    "${workspaceFolder}/Cargo.toml",
                    "--bin",
                    "ai-agent"
                ]
            },
            "terminal": "integrated",
            "presentation": {
                "group": "Debug",
                "order": 1
            }
        }
    ],
    "inputs": [
        {
            "id": "agentArgs",
            "type": "promptString",
            "description": "Аргументы командной строки агента (оставьте пустым для запуска без аргументов)",
            "default": ""
        }
    ]
}