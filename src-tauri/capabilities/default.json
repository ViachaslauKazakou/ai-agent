{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the AI Agent desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
